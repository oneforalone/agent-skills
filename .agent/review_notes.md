# Review Notes

Resolved on 2026-03-12:
- Default install flow now keeps `pua-debugging` opt-in.
- Root `.agent/` scaffold now matches the files referenced by `AGENT.md`.
- `skills/systematic-debugging/CREATION-LOG.md` no longer points at private local paths or dead in-repo skill paths.

Open critical issues:
- None recorded in the current working tree.

Should fix:
- Re-run a full review if more third-party skills are imported.

Follow-up:
- Keep root repository scaffolding and `repo-template/.agent/` aligned when instructions change.

Notes:
- This file is acting as the repository-level review ledger referenced by `AGENT.md`.
