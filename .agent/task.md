# Current Task

Goal:
- Keep the repository bootstrap instructions self-consistent.

Constraints:
- Keep imported skills available without making high-risk behavior part of the default install flow.

Definition of done:
- Root `.agent/` contains the files referenced by `AGENT.md`.
- Default install docs do not auto-install `pua-debugging`.
- Imported skill docs do not reference private local paths or dead in-repo paths.
